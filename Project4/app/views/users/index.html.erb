<h1>Users</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| -%>
    <tr>
      <td><%= link_to "#{user.first_name} #{user.last_name}", photos_path(user.id) %></td>
    </tr>
    <% end -%>
  </tbody>